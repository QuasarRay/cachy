name: Cache CachyOS LXQt offline packages

on:
  push:
    branches: [main, agent/iso-build-run]
    paths:
      - '.github/workflows/cache-offline-packages.yml'
      - 'manifests/**'
      - 'scripts/**'
      - 'pipeline/tools/collect_locked_installer_packages.py'
      - '.o11y/**'
  workflow_dispatch:

permissions:
  contents: read

env:
  CACHE_DIR: .cache/pacman/pkg
  DB_DIR: .cache/pacman/db
  CALAMARES_COMMIT: f1c20a500e14448e36991f1b9d3ebae153827761

jobs:
  cache-packages:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    container:
      image: archlinux:latest
    env:
      BUILDEVENT_APIKEY: ${{ secrets.HONEYCOMB_API_KEY }}
      BUILDEVENT_DATASET: ${{ secrets.HONEYCOMB_DATASET }}
      SENTRY_DSN: ${{ secrets.SENTRY_DSN }}

    steps:
      - name: Check out workspace
        uses: actions/checkout@v4

      - name: Check out locked CachyOS Calamares source
        uses: actions/checkout@v4
        with:
          repository: CachyOS/cachyos-calamares
          ref: ${{ env.CALAMARES_COMMIT }}
          path: locked-calamares
          fetch-depth: 1
          persist-credentials: false

      - name: Bootstrap Arch tools and CachyOS v4 repositories
        shell: bash
        run: |
          set -euo pipefail
          pacman -Syu --noconfirm --needed curl python python-yaml git sudo
          pacman-key --init
          pacman-key --populate archlinux
          pacman-key --recv-keys F3B607488DB35A47 --keyserver keyserver.ubuntu.com
          pacman-key --lsign-key F3B607488DB35A47

          pacman -U --noconfirm \
            https://mirror.cachyos.org/repo/x86_64/cachyos/cachyos-keyring-20240331-1-any.pkg.tar.zst \
            https://mirror.cachyos.org/repo/x86_64/cachyos/cachyos-mirrorlist-27-1-any.pkg.tar.zst \
            https://mirror.cachyos.org/repo/x86_64/cachyos/cachyos-v3-mirrorlist-27-1-any.pkg.tar.zst \
            https://mirror.cachyos.org/repo/x86_64/cachyos/cachyos-v4-mirrorlist-27-1-any.pkg.tar.zst

          python scripts/force-cachyos-v4.py /etc/pacman.conf
          sed -i '/^#\[multilib\]$/,+1 s/^#//' /etc/pacman.conf
          test -f /etc/pacman.d/cachyos-v4-mirrorlist
          pacman -Syy --noconfirm

      - name: Initialize CI observability
        shell: bash
        run: bash .o11y/setup.sh

      - name: Derive installer manifest from locked Calamares tree
        shell: bash
        run: |
          set -euo pipefail
          test "$(git -C locked-calamares rev-parse HEAD)" = "$CALAMARES_COMMIT"
          .o11y/run-step.sh manifest-derivation -- \
            python pipeline/tools/collect_locked_installer_packages.py \
              --calamares-root locked-calamares \
              --repo-root . \
              --output-dir out
          echo "Top-level package/group entries: $(wc -l < out/top-level-packages.txt)"

      - name: Derive content-addressed package cache key
        shell: bash
        run: |
          set -euo pipefail
          package_input_hash="$({
            printf 'calamares=%s\n' "$CALAMARES_COMMIT"
            sha256sum \
              manifests/offline-extras.txt \
              manifests/repo-defaults.txt \
              pipeline/tools/collect_locked_installer_packages.py \
              scripts/force-cachyos-v4.py | sort -k2
          } | sha256sum | cut -d' ' -f1)"
          echo "PACKAGE_CACHE_KEY=cachyos-lxqt-v4-${package_input_hash}" >> "$GITHUB_ENV"
          echo "Package cache key: cachyos-lxqt-v4-${package_input_hash}"

      - name: Restore exact package payload cache
        uses: actions/cache/restore@v4
        with:
          path: |
            .cache/pacman/pkg
            .cache/pacman/db
          key: ${{ env.PACKAGE_CACHE_KEY }}

      - name: Resolve full dependency closure against an empty target database
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$CACHE_DIR" "$DB_DIR/local" "$DB_DIR/sync"
          rm -rf "$DB_DIR/local" "$DB_DIR/sync"
          mkdir -p "$DB_DIR/local" "$DB_DIR/sync"
          pacman -Sy --noconfirm --dbpath "$DB_DIR"
          mapfile -t packages < out/top-level-packages.txt
          .o11y/run-step.sh package-resolution -- \
            pacman -Sp --noconfirm --dbpath "$DB_DIR" \
              --print-format $'%n\t%v\t%r\t%s\t%l' \
              "${packages[@]}" > out/resolved-packages.tsv
          test -s out/resolved-packages.tsv
          echo "Resolved package records: $(wc -l < out/resolved-packages.tsv)"

      - name: Download complete package closure
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t packages < out/top-level-packages.txt
          .o11y/run-step.sh package-download -- \
            pacman -Sw --noconfirm --dbpath "$DB_DIR" \
              --cachedir "$CACHE_DIR" \
              "${packages[@]}"

      - name: Build local offline repository metadata and checksums
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          package_files=("$CACHE_DIR"/*.pkg.tar.zst)
          if (( ${#package_files[@]} == 0 )); then
            echo 'No package payloads were downloaded' >&2
            exit 1
          fi
          rm -f "$CACHE_DIR"/cachyos-lxqt-offline.db* \
                "$CACHE_DIR"/cachyos-lxqt-offline.files*
          .o11y/run-step.sh local-repo-index -- \
            repo-add "$CACHE_DIR/cachyos-lxqt-offline.db.tar.zst" "${package_files[@]}"
          (
            cd "$CACHE_DIR"
            sha256sum -- *.pkg.tar.zst | sort -k2 > SHA256SUMS
          )
          {
            echo "package_files=${#package_files[@]}"
            echo "cache_bytes=$(du -sb "$CACHE_DIR" | cut -f1)"
            echo "cache_human=$(du -sh "$CACHE_DIR" | cut -f1)"
            echo "source_sha=${GITHUB_SHA}"
            echo "calamares_commit=${CALAMARES_COMMIT}"
            echo "package_cache_key=${PACKAGE_CACHE_KEY}"
          } | tee out/cache-summary.txt

      - name: Save exact package cache for ISO job
        uses: actions/cache/save@v4
        with:
          path: |
            .cache/pacman/pkg
            .cache/pacman/db
          key: ${{ env.PACKAGE_CACHE_KEY }}

      - name: Upload review manifests
        uses: actions/upload-artifact@v4
        with:
          name: cachyos-lxqt-package-manifests-v4-${{ github.run_id }}
          path: out/
          if-no-files-found: error
          retention-days: 14

      - name: Report cache workflow failure
        if: failure()
        continue-on-error: true
        shell: bash
        run: |
          PY=python
          if [ -x .o11y/.venv/bin/python ]; then PY=.o11y/.venv/bin/python; fi
          "$PY" .o11y/report_failure.py --job cache-packages

      - name: Finalize cache observability
        if: always()
        continue-on-error: true
        shell: bash
        run: bash .o11y/finalize.sh "${{ job.status }}"

      - name: Upload cache diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: o11y-cache-${{ github.run_id }}
          path: .o11y/runtime/
          if-no-files-found: ignore
          retention-days: 14
